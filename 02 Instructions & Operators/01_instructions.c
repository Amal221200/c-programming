#include <stdio.h>

int main() {
  // Type Instructions
  int a;
  int b = 9;
  
  // Arithmatic Instructiona
  a = b + 34;
  
  // Control Instruction
  // 1. Sequence Control Instructions (Line By Line Execution, Default mode)
  // 2. Decision Control Instructions (if else statement)
  // 3. Loop Control Instructions (for loop, while loop, etc)
  // 4. Case control Instructions (Switch Case)
  return 0;
}
